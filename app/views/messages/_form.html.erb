<%= simple_form_for(@message, html: {class: 'form-inline' }) do |f|%>
    <%= f.input :text, label: "Message content", as: :text, :input_html => { :cols => 5, :rows => 5} %>
    <%= f.input :campaign_id, :as => :hidden %>
    <%= f.button :submit, value: "Next      →" %>
<% end %>
