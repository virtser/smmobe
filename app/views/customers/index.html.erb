<h1>Choose customers</h1>

<%= render 'campaigns/steps', :locals => {:step1 => "passed",
                                          :step2 => "passed",
                                          :step3 => "selected"} %>

<div>
    <%= link_to 'Manually Add Customers', new_customer_path, class: 'btn' %>
    <%= link_to 'Import Customers from CSV', import_path, class: 'btn' %>
    <%= link_to 'Import from CRM', '#', class: 'btn' %>
</div>

<div style="font-style: italic; color: red;"><%= flash[:notice] %></div>

<br/>

<% if !@customers.nil? && @customers.count > 0 %>
    <h2>Customers</h2>
    <%= render 'customers' %>

    <input class="btn" name="commit" type="submit" value="Next      →"
           onclick="location.href='/reviews/<%= flash[:campaign_id] %>'"/>
<% end %>